import { ChildDDirective } from './child-d.directive';

describe('ChildDDirective', () => {
  it('should create an instance', () => {
    const directive = new ChildDDirective();
    expect(directive).toBeTruthy();
  });
});
